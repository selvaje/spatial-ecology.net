- name: Copy Sphinx docs into _site
  run: |
    if [ -d "docs/build/html" ]; then
      mkdir -p _site/docs/build/html
      cp -r docs/build/html/. _site/docs/build/html/
      touch _site/docs/build/html/.nojekyll
    fi